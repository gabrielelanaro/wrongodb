# IDEAS

- SQL on top of WiredTiger: WrongoDB could potentially expose both a SQL interface and a MongoDB-compatible interface on the same storage core. Interesting project, though it may not be fully possible in a clean way.
